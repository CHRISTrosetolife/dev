import {html_p_text} from "./html_p_text.mjs";
import {html_element} from "./html_element.mjs";
export function lesson_console_log_analogy(parent) {
    let text = 'a computer program has three parts :';
    html_p_text(parent, text);
}
