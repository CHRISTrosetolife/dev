import { prayers_list_generic } from "./prayers_list_generic.mjs";
export function prayers_list_generic_5(list, on_first, on_last) {
  let count = 5;
  let v = prayers_list_generic(list, count, on_first, on_last);
  return v;
}
