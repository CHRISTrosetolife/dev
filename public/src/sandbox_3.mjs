import { ceb_bible_words_count_cache_new } from "./ceb_bible_words_count_cache_new.mjs";
export async function sandbox_3() {
  let words = await ceb_bible_words_count_cache_new();
  let count = 500;
  each_range_1(count, (i) => {});
}
