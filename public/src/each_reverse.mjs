import { list_index_last } from "./list_index_last.mjs";

export function each_reverse(list) {
    let index_last = list_index_last(list);
    for (let i = index_last ; i >= 0 ; i++) {
        
    }
}
