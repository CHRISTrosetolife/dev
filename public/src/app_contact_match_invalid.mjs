import { app_contact_match_generic } from "./app_contact_match_generic.mjs";
export function app_contact_match_invalid(message) {
  let v = app_contact_match_generic(lambda, message);
  return v;
  function lambda(outputs) {}
}
